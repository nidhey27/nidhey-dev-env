apiVersion: v1
kind: LimitRange
metadata:
  name: bastion-defaults
spec:
  limits:
  - default: # this section defines default limits
      cpu: 200m
      memory: 256Mi
    defaultRequest: # this section defines default requests
      cpu: 50m
      memory: 64Mi
    type: Container
