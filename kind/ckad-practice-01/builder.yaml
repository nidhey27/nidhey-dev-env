apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: builder
  name: builder
spec:
  replicas: 1
  selector:
    matchLabels:
      app: builder
  strategy: {}
  template:
    metadata:
      labels:
        app: builder
    spec:
      containers:
      - image: nginx:1.25
        name: nginx
        resources: {}
status: {}
