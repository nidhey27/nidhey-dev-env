# Brewfile — declarative list of Homebrew packages.
# Apply with:  brew bundle --file=Brewfile
# Regenerate from current machine with:  brew bundle dump --force --file=Brewfile

# --- Formulae -------------------------------------------------------------
brew "cmake"
brew "colima"          # container runtime (Docker VM) for macOS
brew "copier"          # project scaffolding
brew "docker"
brew "docker-compose"
brew "fzf"             # fuzzy finder — used by kexec/kubectx pickers
brew "glab"            # GitLab CLI
brew "helm"
brew "kubectx"         # `kubectx` + `kubens` — fast context/namespace switching
brew "mise"            # runtime/tool version manager (drives .config/mise/config.toml)
brew "pkgconf"
brew "tree"
brew "watch"
brew "zsh"

# --- Casks ----------------------------------------------------------------
cask "clipy"           # clipboard manager
